def count_vowels(text):
    count = 0
    for ch in text:
        if ch in "aeiouAEIOU":
            count = count + 1
    return count

sentence = input("Enter a sentence: ")
print("Number of vowels:", count_vowels(sentence))
