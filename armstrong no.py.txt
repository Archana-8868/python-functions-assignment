def check_armstrong(num):
    original = num
    total = 0
    while num > 0:
        digit = num % 10
        total = total + digit * digit * digit
        num = num // 10
    return original == total

number = int(input("Enter number: "))
print("Armstrong:", check_armstrong(number))
